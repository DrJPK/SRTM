% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-trajType.R
\name{srtm_compute_trajType}
\alias{srtm_compute_trajType}
\title{Compute semantic trajectory types from slopes and groups}
\usage{
srtm_compute_trajType(
  data,
  slope_col = "m01",
  group_col = "trajGroup",
  label_scheme = c("arrows", "signs", "text"),
  dt01 = NULL,
  steep_const = 0.25,
  flat_const = 0.05,
  outcome_cols = NULL
)
}
\arguments{
\item{data}{A data frame containing at least:
\itemize{
\item a numeric slope column (e.g. \code{m01}), and
\item a factor grouping column (e.g. \code{trajGroup}).
If available, the columns named in \code{outcome_cols} are used to
estimate the outcome scale range, and \code{dt01} is used (or estimated)
as the time difference between the first two measurement occasions.
}}

\item{slope_col}{Name of the numeric column in \code{data} containing
the slope for the first time interval (typically \code{m01}). Can be a
string; defaults to \code{"m01"}.}

\item{group_col}{Name of the factor column in \code{data} that indexes
trajectory groups (typically \code{trajGroup}). Can be a string;
defaults to \code{"trajGroup"}.}

\item{label_scheme}{Character string specifying how the trajectory
types should be labelled. One of:
\itemize{
\item \code{"arrows"} — unicode arrows (↟, ↑, →, ↓, ↡),
\item \code{"signs"}  — sign-like labels (\code{"++"}, \code{"+"}, \code{"—"}, \code{"-"}, \code{"--"}),
\item \code{"text"}   — textual labels (\code{"steep_pos"}, \code{"shallow_pos"},
\code{"flat"}, \code{"shallow_neg"}, \code{"steep_neg"}).
}}

\item{dt01}{Optional numeric giving the time interval between the first
and second measurement occasions. If \code{NULL}, the function will:
\itemize{
\item use the median of \code{data$dt01} when present, or
\item default to \code{1} otherwise.
}}

\item{steep_const}{Numeric constant (default \code{0.25}) controlling the
threshold for defining \emph{steep} vs \emph{shallow} trajectories. The
threshold in slope units is calculated as
\code{steep_const * scale_range / dt01}, where \code{scale_range} is the
approximate range of the outcome scale.}

\item{flat_const}{Numeric constant (default \code{0.05}) controlling the
threshold for defining \emph{flat} trajectories around zero change. The
band in slope units is calculated as
\verb{± flat_const * scale_range / dt01}.}

\item{outcome_cols}{Optional character vector naming columns in \code{data}
that represent the outcome scale (e.g., c("y0","y1","y2")). When
provided, the combined range of these columns is used to estimate
\code{scale_range}. If \code{NULL}, the function defaults to
\code{c("y0", "y1", "y2")} and uses the intersection with \code{names(data)};
if none are found, it falls back to the range of \code{slope * dt01}.}
}
\description{
Internal helper used by \code{\link[=SRTMAnalyse]{SRTMAnalyse()}} to convert raw trajectory
slopes (e.g., \code{m01}) and grouping factors (e.g., \code{trajGroup}) into
semantically meaningful trajectory types such as \emph{steep positive},
\emph{shallow negative}, or \emph{flat}. The function classifies each
trajectory group based on its mean slope and returns a factor
\code{trajType} aligned with the rows of \code{data}.
}
\keyword{internal}
