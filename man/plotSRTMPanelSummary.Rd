% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotSRTMPanel.R
\name{plotSRTMPanelSummary}
\alias{plotSRTMPanelSummary}
\title{Summary panel plot of SRTM trajectories (box/violin by time)}
\usage{
plotSRTMPanelSummary(
  data,
  id_col = "ID",
  facet = TRUE,
  palette = c("simple", "pastel", "modern", "colourblind", "greys"),
  geom = c("boxplot", "violin"),
  width = 0.35,
  dodge_width = 0.5,
  expected_fill = "white",
  show_legend = TRUE
)
}
\arguments{
\item{data}{A data frame, typically the \code{data} component of an
\code{\link[=SRTMAnalyse]{SRTMAnalyse()}} result. Must contain \code{y0}, \code{y1}, \code{y2}, \code{baseGroup},
and either \code{trajType} or \code{trajGroup}. If \code{exp_y2} is present it will
be used as expected Post scores.}

\item{id_col}{Name of the ID column (unused for aggregation, but kept
for consistency with \code{\link[=plotSRTMPanel]{plotSRTMPanel()}}). Default \code{"ID"}.}

\item{facet}{Logical; if \code{TRUE} (default) facet rows by baseline and
columns by trajectory direction.}

\item{palette}{Palette name passed to \code{\link[=setPlotPalette]{setPlotPalette()}}.
One of \code{"simple"}, \code{"pastel"}, \code{"modern"}, \code{"colourblind"}, \code{"greys"}.}

\item{geom}{Character; \code{"boxplot"} (default) or \code{"violin"}.}

\item{width}{Numeric width of each box/violin (passed to the geom).}

\item{dodge_width}{Numeric total dodge width between observed and
expected at Post.}

\item{expected_fill}{Fill colour used for expected distributions
(default \code{"white"}).}

\item{show_legend}{Logical; if \code{TRUE} (default), a simple custom legend
is added below the panel indicating which glyph corresponds to
observed vs expected scores (grey square with black border for
observed; white square with grey border for expected).}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object. If \code{show_legend = TRUE}, this is a
patchwork object combining the main panel and a small legend row.
}
\description{
Creates a facetted panel plot showing the distribution of scores at each
time point (Historical, Pre, Post) for each Baseline × Trajectory cell in
an SRTM analysis, using either boxplots or violins. Observed scores are
filled with the usual combined SRTM palette colour, and expected post
scores (exp_y2) are shown as white-filled, coloured outlines, dodged
horizontally at the Post time point.
}
