% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importSRTMExcel.R
\name{importSRTMExcel}
\alias{importSRTMExcel}
\title{Import SRTM Data from an Excel Spreadsheet}
\usage{
importSRTMExcel(
  path = NULL,
  sheet = NULL,
  interactive = TRUE,
  ID = NULL,
  y0 = NULL,
  y1 = NULL,
  y2 = NULL,
  extra_vars = NULL,
  missing_tokens = NULL
)
}
\arguments{
\item{path}{Optional. Path to the Excel file. If \code{NULL} and
\code{interactive = TRUE}, a file chooser dialog is opened.}

\item{sheet}{Optional. Sheet name or index to read, passed to
\code{\link[readxl:read_excel]{readxl::read_excel()}}. If \code{NULL}, the first sheet is used.}

\item{interactive}{Logical. If \code{TRUE} (default), the user may be prompted to
choose the file and/or select the appropriate columns interactively.}

\item{ID}{Optional. Name of the column containing learner identifiers. If
\code{NULL} and interactive mode is enabled, the user is prompted to choose.}

\item{y0}{Optional. Name of the column containing \strong{historical data}. If
\code{NULL} and interactive mode is enabled, the user is prompted to choose.}

\item{y1}{Optional. Name of the column containing \strong{baseline} data. If
\code{NULL} and interactive mode is enabled, the user is prompted to choose.}

\item{y2}{Optional. Name of the column containing \strong{post} data. If \code{NULL}
and interactive mode is enabled, the user is prompted to choose.}

\item{extra_vars}{Optional character vector or list of additional variable
names to import. Any that are present in the dataset are included and
coerced to factors to support downstream grouping or filtering.}

\item{missing_tokens}{Optional character vector or list of additional
codes that should be treated as missing values. These codes are merged
with a default set of common missing tokens such as \code{""}, \code{"NA"}, \code{"N/A"},
\code{"."}, and \code{"-"}. When \code{interactive = TRUE} and \code{missing_tokens} is \code{NULL},
users can optionally add extra codes via a simple prompt.}
}
\value{
A tibble containing:
\itemize{
\item \code{ID} — factor (participant identifier)
\item \code{y0} — numeric historical data
\item \code{y1} — numeric baseline data
\item \code{y2} — numeric post data
\item Additional factor variables if \code{extra_vars} were provided
}
}
\description{
Reads a teacher or researcher provided Excel file containing Self-Referenced Trajectory
Modelling (SRTM) data and converts it into a standardised dataframe with the
required variables:
\itemize{
\item \code{ID} – learner or participant identifier (factor)
\item \code{y0} – historical data
\item \code{y1} – baseline data
\item \code{y2} – post data
}

The function supports interactive selection of the file and relevant columns,
making it suitable for teachers and non-technical users. Any additional
variables named via \code{extra_vars} are automatically coerced to factors.
}
\details{
The function wraps \code{\link[readxl:read_excel]{readxl::read_excel()}} and provides an interactive column
selection process based on \code{utils::menu()}. Users can load spreadsheets
exported from school systems or teacher-created Excel files without requiring
exact variable naming.

Before coercing the outcome columns to numeric, common spreadsheet codes for
missing values (e.g., \code{"NA"}, \code{"N/A"}, \code{"."}, \code{"-"}, \code{"Missing"}) and any
user-specified \code{missing_tokens} are converted to \code{NA}. Any remaining
non-numeric values that cannot be coerced result in \code{NA} with a warning.

The returned dataframe always contains \strong{ID}, \strong{y0}, \strong{y1}, and \strong{y2}
with standard names suitable for downstream SRTM analysis functions.
}
\examples{
\dontrun{
# Interactive import (user selects file + columns)
df <- importSRTMExcel()

# Non-interactive import with explicit column names
df <- importSRTMExcel(
  path = "teacher_data.xlsx",
  ID   = "StudentID",
  y0   = "Prior_Score",
  y1   = "Baseline",
  y2   = "Post",
  extra_vars     = c("Class", "Teacher"),
  missing_tokens = c("Not tested", "Absent")
)
}

}
