% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-srtm_analysis.R
\name{summary.srtm_analysis}
\alias{summary.srtm_analysis}
\title{Summary method for SRTM analysis objects}
\usage{
\method{summary}{srtm_analysis}(object, sort_by = c("p", "abs_diff", "none"), ...)
}
\arguments{
\item{object}{An object of class \code{"srtm_analysis"}.}

\item{sort_by}{Character string indicating how to sort the summary.
One of:
\itemize{
\item \code{"p"} (default): ascending p-value for outcome comparisons
(falls back to slope p-values if outcome p-values are not present),
\item \code{"abs_diff"}: descending absolute difference between observed
and expected means (\code{mean_diff} if available, otherwise
the absolute outcome difference),
\item \code{"none"}: no additional sorting.
}}

\item{...}{Ignored, included for method compatibility.}
}
\value{
A tibble with one row per Base × Trajectory type combination, including:
\itemize{
\item grouping variables (\code{baseGroup}, \code{trajType});
\item counts and proportions from \code{GroupSummary};
\item mean observed and expected outcomes and their difference
(typically \code{mean_obs}, \code{mean_exp}, \code{mean_diff});
\item (if available) outcome comparison statistics:
\code{t_outcome}, \code{diff_outcome}, \code{p_outcome},
\code{df_outcome};
\item (if available) slope comparison statistics:
\code{t_slope}, \code{diff_slope}, \code{p_slope},
\code{df_slope}.
}

The tibble is printed and also returned invisibly.
}
\description{
Produces a merged summary table of group-wise outcomes and statistical
comparisons for an \code{"srtm_analysis"} object. This combines the
group-level summaries (from \code{\link[=summariseGroupOutcomes]{summariseGroupOutcomes()}}) with the
t-test-style comparisons for outcomes (from \code{\link[=compareOutcomes]{compareOutcomes()}}) and,
if available, slope comparisons (from \code{\link[=compareSlopes]{compareSlopes()}}).

Grouping is shown using \code{baseGroup} and \code{trajType} (older objects that
still use \code{trajGroup} are normalised to \code{trajType} for display).
}
