% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculateTrajectories.R
\name{calculateTrajectories}
\alias{calculateTrajectories}
\title{Calculate individual trajectory slopes between two time points}
\usage{
calculateTrajectories(
  data,
  y0 = "y0",
  y1 = "y1",
  time_period,
  interactive = TRUE
)
}
\arguments{
\item{data}{A data frame or tibble containing the variables specified by
\code{y0} and \code{y1}.}

\item{y0}{Column specification for the earlier measurement (e.g.,
historical or baseline score). Can be a column name as a string (such
as \code{"y0"}) or a bare column name (such as \code{y0}). Defaults to \code{"y0"}.}

\item{y1}{Column specification for the later measurement (e.g., baseline or
post score). Can be a column name as a string or a bare column name.
Defaults to \code{"y1"}.}

\item{time_period}{Numeric scalar giving the elapsed time between \code{y0} and
\code{y1} (e.g., number of days, weeks, or terms). If missing and
\code{interactive = TRUE}, the user is prompted to either enter a time period
directly or supply two dates from which a difference in days is
computed. If missing and \code{interactive = FALSE}, an error is raised.}

\item{interactive}{Logical. If \code{TRUE} (default), interactive prompts are
used when \code{time_period} is not supplied. If \code{FALSE}, \code{time_period} must
be provided explicitly.}
}
\value{
A numeric vector of length \code{nrow(data)} containing the slopes between \code{y0}
and \code{y1}, using the supplied (or interactively determined) \code{time_period}.
}
\description{
Computes the trajectory slope between two numeric variables (for example
\code{y0} and \code{y1}) for each individual in a dataset, given the time elapsed
between those measurements. The result is a numeric vector that can be
attached to the original data (e.g., as \code{m01}).
}
\details{
The function verifies that \code{data} is a data frame, that \code{y0} and \code{y1}
exist and are numeric, and that the resolved \code{time_period} is a single
positive numeric value. When \code{time_period} is not supplied and
\code{interactive = TRUE}, the user can:

\itemize{
\item enter a time period directly (e.g., \code{"90"} days), or
\item enter two ISO-format dates (YYYY-MM-DD), in which case the time
period is computed as the difference in days.
}

The trajectory slope for each row is then computed as
\deqn{
  m_{01} = \frac{y_1 - y_0}{\text{time\_period}}.
}

No imputation is performed; if \code{y0} or \code{y1} contain \code{NA}, the corresponding
slope will also be \code{NA}.
}
\examples{
# Using synthetic data from SRTM
df <- generateSynthData(n = 10, seed = 123)

# Suppose y0 and y1 are 90 days apart
df$m01 <- calculateTrajectories(
  data        = df,
  y0          = "y0",
  y1          = "y1",
  time_period = 90,
  interactive = FALSE
)

}
