% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictPostResponse.R
\name{predictPostResponse}
\alias{predictPostResponse}
\title{Predict post measurement using group-specific linear trajectories}
\usage{
predictPostResponse(
  data,
  y0 = "y0",
  y1 = "y1",
  y2 = "y2",
  base_group = "baseGroup",
  traj_group = "trajGroup",
  time01,
  time12
)
}
\arguments{
\item{data}{A data frame or tibble containing the specified columns.}

\item{y0, y1, y2}{Column specifications (names or tidy-select) for the three
time points.}

\item{base_group}{Column specification for the baseline grouping variable
(e.g. \code{"baseGroup"}).}

\item{traj_group}{Column specification for the trajectory grouping variable
(e.g. \code{"trajGroup"}).}

\item{time01}{Numeric scalar giving the elapsed time between \code{y0} and \code{y1}.}

\item{time12}{Numeric scalar giving the elapsed time between \code{y1} and \code{y2}.}
}
\value{
The input \code{data} with additional numeric columns:
\itemize{
\item \code{intercept01} — intercept from the group-specific model \code{y ~ t}.
\item \code{slope01} — slope from the group-specific model between \code{y0} and \code{y1}.
\item \code{exp_y2} — expected value at the time of \code{y2}, computed as
\code{y1 + slope01 * time12}.
}
}
\description{
Fits simple linear models within each combination of baseline group and
trajectory group using \code{y0} and \code{y1}, then uses the resulting group-specific
slopes to generate an expected post score at the time of \code{y2}.
}
