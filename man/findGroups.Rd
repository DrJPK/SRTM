% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/findGroups.R
\name{findGroups}
\alias{findGroups}
\title{Find latent groups at a given time point using kernel density estimation}
\usage{
findGroups(
  data,
  time_var = "y1",
  interactive = TRUE,
  show_plot = interactive,
  bw = "nrd0",
  adjust = 1,
  adjust_grid = c(0.5, 1, 2)
)
}
\arguments{
\item{data}{A data frame or tibble containing at least one numeric column
representing the time point of interest (e.g. \code{y1}).}

\item{time_var}{A column specification (name or tidy-selection) indicating
which variable in \code{data} should be used for group finding. Defaults to
\code{"y1"}.}

\item{interactive}{Logical. If \code{TRUE} (default), the user is given the
opportunity to override the suggested number of groups via a console
prompt.}

\item{show_plot}{Logical. If \code{TRUE}, a \code{ggplot2} density plot is printed
showing the KDE curves for multiple bandwidth adjustments and the local
minima for the main adjustment. Defaults to the value of \code{interactive}.}

\item{bw}{Bandwidth argument passed to \code{\link[stats:density]{stats::density()}}. Can be a numeric
bandwidth or a character string such as \code{"nrd0"} (the default).}

\item{adjust}{Numeric scaling factor for the KDE bandwidth passed to
\code{\link[stats:density]{stats::density()}}. Values greater than 1 give smoother curves, values
less than 1 give more detailed curves.}

\item{adjust_grid}{Numeric vector of additional \code{adjust} values used for
bandwidth diagnostics. KDEs are fitted for each value in \code{adjust_grid}
and overlaid in the plot (if \code{show_plot = TRUE}), and a small summary of
detected minima and suggested group counts is returned.}
}
\value{
An object of class \code{"srtm_group_suggestion"} (a list) with elements:

\itemize{
\item \code{nGroups} — the final number of groups to use (possibly modified by the user).
\item \code{suggested_nGroups} — the initial number of groups suggested from the main KDE.
\item \code{minima_x} — numeric vector of local minima positions (for the main \code{adjust}).
\item \code{time_var} — character string naming the variable used for grouping.
\item \code{density} — the main \code{\link[stats:density]{stats::density()}} object for the chosen \code{bw} and \code{adjust}.
\item \code{bw} — the bandwidth argument used.
\item \code{adjust} — the main bandwidth adjustment factor.
\item \code{bw_diagnostic} — a tibble summarising, for each value in
\code{adjust_grid}, the number of minima, suggested number of groups, and
the corresponding minima locations.
}

This object is intended to be passed to \code{\link[=assignGroups]{assignGroups()}} to perform the
actual group assignment.
}
\description{
Uses kernel density estimation (KDE) on a single time point (e.g. baseline
\code{y1}) to suggest how many latent groups may be present in the data. Local
minima of the estimated density are treated as potential boundaries between
groups. The function can optionally show a diagnostic density plot and allow
the user to confirm or override the suggested number of groups.
}
\details{
For the selected \code{time_var}, the function:

\enumerate{
\item Computes a kernel density estimate using \code{\link[stats:density]{stats::density()}} with the
specified \code{bw} and \code{adjust}.
\item Approximates the derivative of the density and identifies local
minima (points where the derivative changes sign from negative to
positive).
\item Uses the number of local minima to propose a suggested number of
groups: if there are \\eqn{k} minima, \\eqn{k + 1} groups are suggested;
if no minima are found, a single group is suggested.
\item Repeats the KDE/minima detection for each value in \code{adjust_grid} to
provide a bandwidth-sensitivity diagnostic.
}

When \code{show_plot = TRUE} and \code{ggplot2} is available, the function prints a
density plot for all adjustments in \code{adjust_grid}, coloured by \code{adjust},
with dashed vertical lines marking the local minima for the main \code{adjust}
value. This allows users to visually inspect how stable the suggested group
structure is with respect to bandwidth choices.

If \code{interactive = TRUE}, the user is prompted in the console to accept the
suggested number of groups or enter a different value. The final choice is
stored in the returned object.
}
\examples{
# Using synthetic data
df <- generateSynthData(n = 300, seed = 123)

# Non-interactive: get suggested groups for y1
gp <- findGroups(
  data        = df,
  time_var    = "y1",
  interactive = FALSE,
  show_plot   = FALSE
)

gp$nGroups
gp$bw_diagnostic

\dontrun{
# Interactive, with plot, suitable for teacher-facing workflows
gp <- findGroups(df, time_var = "y1", interactive = TRUE, show_plot = TRUE)

# Use the result to assign groups later:
df$Group <- assignGroups(df, group_params = gp)
}

}
