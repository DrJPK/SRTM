% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotSRMTCutPoints.R
\name{plotSRTMCutPoints}
\alias{plotSRTMCutPoints}
\title{Plot saved cut-points from an SRTM analysis object}
\usage{
plotSRTMCutPoints(x, which = NULL, ...)
}
\arguments{
\item{x}{An object of class \code{"srtm_analysis"}, typically the output of
\code{\link[=SRTMAnalyse]{SRTMAnalyse()}}.}

\item{which}{Optional character or integer identifying which stored
group suggestion to plot. If \code{NULL} (default), a console menu is
shown listing all available suggestions and the user is prompted to
choose one.}

\item{...}{Additional arguments passed on to \code{\link[=plotCutPoints]{plotCutPoints()}}, such
as \code{x_label}, \code{annotate}, \code{decimals}, \code{shade}, or \code{palette}.}
}
\value{
A \code{ggplot} object produced by \code{\link[=plotCutPoints]{plotCutPoints()}}. The plot is not
printed automatically, so it can be composed with other plots (e.g.
via patchwork).
}
\description{
Convenience wrapper for \code{\link[=plotCutPoints]{plotCutPoints()}} that works on an
\code{"srtm_analysis"} object returned by \code{\link[=SRTMAnalyse]{SRTMAnalyse()}}. It looks up any
stored \code{srtm_group_suggestion} objects (from calls to \code{\link[=findGroups]{findGroups()}}
inside \code{SRTMAnalyse()}), allows the user to choose one via a simple
menu (if \code{which} is not supplied), and then produces the corresponding
density + cut-point plot.
}
\details{
\code{SRTMAnalyse()} stores all internal \code{\link[=findGroups]{findGroups()}} results in the
\code{group_params} component of its output, as a list with elements:

\itemize{
\item \code{baseline_overall} — overall baseline grouping on \code{y1}
(if used).
\item \code{traj_overall} — overall trajectory grouping on \code{m01}
(if used).
\item \code{traj_by_base} — a named list of \code{srtm_group_suggestion}
objects for each \code{baseGroup} level (grouping on \code{m01}).
\item \code{baseline_by_traj} — a named list of \code{srtm_group_suggestion}
objects for each \code{trajGroup} level (grouping on \code{y1}).
}

This helper flattens these into a single named vector of choices, such
as:

\itemize{
\item \code{"Baseline (overall)"},
\item \code{"Trajectory (overall)"},
\item \code{"Trajectory within baseline A"},
\item \code{"Trajectory within baseline B"},
\item \code{"Baseline within trajectory ↟"}, etc.
}

If \code{which} is:
\itemize{
\item \code{NULL} — a menu of these labels is shown using
\code{\link[utils:menu]{utils::menu()}}, and the user’s choice is used.
\item a character string — it is matched against the available
labels (exact match).
\item an integer — it is treated as a 1-based index into the list
of available labels.
}

The selected \code{srtm_group_suggestion} object is then passed directly to
\code{\link[=plotCutPoints]{plotCutPoints()}}, with any additional \code{...} arguments forwarded.
}
\examples{
\dontrun{
x <- SRTMAnalyse(SRTM_synth_data, group_first = "baseline")

# interactive choice via menu
p <- plotSRTMCutPoints(x)
p

# or explicitly:
p2 <- plotSRTMCutPoints(x, which = "Baseline (overall)")
p3 <- plotSRTMCutPoints(x, which = 2)  # second item in the menu
}

}
