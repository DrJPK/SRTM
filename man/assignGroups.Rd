% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assignGroups.R
\name{assignGroups}
\alias{assignGroups}
\title{Assign group labels based on a group suggestion object}
\usage{
assignGroups(data, group_params = NULL, interactive = TRUE)
}
\arguments{
\item{data}{A data frame or tibble containing the time-point variable named
in \code{group_params$time_var}.}

\item{group_params}{An object of class \code{"srtm_group_suggestion"}, typically
returned by \code{\link[=findGroups]{findGroups()}}. It must contain at least:
\itemize{
\item \code{time_var} — the name of the variable to use for grouping.
\item \code{nGroups} — the final number of groups to form.
\item \code{minima_x} — the positions of local minima in the density estimate.
}
If \code{group_params} is \code{NULL} or not of the correct class and
\code{interactive = TRUE}, the function calls \code{\link[=findGroups]{findGroups()}} interactively to
construct a suitable object.}

\item{interactive}{Logical. If \code{TRUE} (default), and \code{group_params} is not
supplied or invalid, the user is guided through an interactive process to
select a time variable and determine the number of groups via \code{\link[=findGroups]{findGroups()}}.
If \code{FALSE}, \code{group_params} must be a valid \code{"srtm_group_suggestion"} object.}
}
\value{
A factor vector of length \code{nrow(data)} containing group labels:
\code{"A"}, \code{"B"}, \code{"C"}, … up to the
}
\description{
Assigns group membership to each case in a dataset using the information
stored in an \code{"srtm_group_suggestion"} object produced by \code{\link[=findGroups]{findGroups()}}.
The grouping is based on a single numeric time point (e.g. \code{y1}), with
cut-points derived from the local minima of a kernel density estimate.
}
\details{
The function uses the local minima stored in \code{group_params$minima_x} as
potential boundaries between groups. If the number of minima exceeds
\code{nGroups - 1}, a subset of minima is selected to provide approximately
evenly spaced cut-points across the distribution. These cut-points are then
used to partition the chosen time variable into \code{nGroups} ordered categories,
labelled \code{"A"}, \code{"B"}, \code{"C"}, and so on.

Cases with missing values on the grouping variable are assigned \code{NA} for the
returned group label. If there are fewer non-missing observations than
\code{nGroups}, or there are insufficient minima to support the requested number
of groups, an error is raised.
}
