% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotCuts.R
\name{plotCutPoints}
\alias{plotCutPoints}
\title{Plot density and cut-points from an SRTM group suggestion}
\usage{
plotCutPoints(
  group_params,
  x_label = NULL,
  annotate = TRUE,
  decimals = 2L,
  shade = TRUE,
  palette = c("simple", "pastel", "modern", "colourblind", "greys")
)
}
\arguments{
\item{group_params}{An object of class \code{"srtm_group_suggestion"},
typically the result of \code{\link[=findGroups]{findGroups()}}. It must contain at least:
\itemize{
\item \code{density} — the main \code{\link[stats:density]{stats::density()}} object,
\item \code{minima_x} — numeric vector of local minima locations,
\item \code{nGroups} — final number of groups used,
\item \code{time_var} — name of the variable used for grouping,
\item \code{bw} — bandwidth argument passed to \code{\link[stats:density]{stats::density()}},
\item \code{adjust} — bandwidth adjustment factor.
}}

\item{x_label}{Optional character string for the x-axis label. If
\code{NULL} (default), a label is chosen based on \code{group_params$time_var}:
\code{"baseline score"} for \code{"y1"}, \code{"historical slope"} for \code{"m01"},
otherwise the raw \code{time_var} name.}

\item{annotate}{Logical. If \code{TRUE} (default), annotations showing the
numeric x-coordinates of the cut-points are added near the top of
the panel.}

\item{decimals}{Integer number of decimal places to show in the
cut-point annotations. Default is \code{2}.}

\item{shade}{Logical. If \code{TRUE} (default), the density is filled with
semi-transparent coloured bands corresponding to the groups implied
by the cut-points. If \code{FALSE}, only the (unfilled) density curve and
vertical cut-lines are drawn.}

\item{palette}{Character palette name passed to \code{\link[=setPlotPalette]{setPlotPalette()}}
to ensure visual consistency with other SRTM plots. One of
\code{"simple"}, \code{"pastel"}, \code{"modern"}, \code{"colourblind"}, \code{"greys"}.}
}
\value{
A \code{ggplot} object showing the density, cut-points, and (optionally)
shaded group regions and annotated cut-point values. The plot is not
printed automatically, making it suitable for composition with
patchwork or other plotting workflows.
}
\description{
Produces a publication-ready density plot showing the kernel density
estimate and the cut-points implied by an \code{"srtm_group_suggestion"}
object returned by \code{\link[=findGroups]{findGroups()}}. This is useful for reporting how
baseline or slope-based groups were defined, and can be combined with
other plots using patchwork.
}
\details{
This function does \strong{not} recompute kernel densities. It uses the
density stored in \code{group_params$density} (the main density for the
chosen \code{bw} and \code{adjust}) and the minima stored in
\code{group_params$minima_x}.

The cut-points that define groups are reconstructed using the same
logic as \code{\link[=assignGroups]{assignGroups()}}:

\enumerate{
\item If \code{nGroups == 1}, no cut-points are used and the entire
distribution is treated as a single group.
\item Otherwise, the number of minima must be at least \code{nGroups - 1}.
\item If there are more minima than needed, a subset is chosen by
spreading indices approximately evenly across the minima,
giving \code{nGroups - 1} cut-points.
}

When \code{shade = TRUE}, the area under the density curve is partitioned
into \code{nGroups} contiguous regions using these cut-points, and filled
using colours derived from \code{\link[=setPlotPalette]{setPlotPalette()}}. The density line itself
is drawn in black for clarity.

Vertical dashed lines are drawn at each cut-point. When
\code{annotate = TRUE}, the x-coordinates of the cut-points are shown as
text labels near the top of the plot (formatted to \code{decimals}
decimal places).
}
\examples{
\dontrun{
df <- generateSynthData(n = 300, seed = 123)
gp <- findGroups(df, time_var = "y1", interactive = FALSE, show_plot = FALSE)

p <- plotCutPoints(gp, x_label = "Baseline score")
p

# combine multiple cut-point plots with patchwork
gp_y1  <- findGroups(df, time_var = "y1", interactive = FALSE, show_plot = FALSE)
gp_m01 <- findGroups(df, time_var = "m01", interactive = FALSE, show_plot = FALSE)
p1 <- plotCutPoints(gp_y1,  x_label = "Baseline score")
p2 <- plotCutPoints(gp_m01, x_label = "Historical slope")
p1 + p2
}

}
